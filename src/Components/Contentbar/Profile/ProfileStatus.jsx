import React, { useEffect } from 'react';

const ProfileStatus = (props) => {

    useEffect(() => {
        
    })

    return (
        <div>
            <input value={status} onChange={} ></input>
        </div>
    )
}

export default ProfileStatus;